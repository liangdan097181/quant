1. 安装项目依赖：使用pip3安装requirements.txt中的所有依赖包
2. 运行Streamlit应用：启动Web服务器
3. 验证应用是否正常运行：检查输出的URL并确认服务启动

**具体步骤**：

1. 执行命令：`pip3 install -r requirements.txt`
2. 执行命令：`streamlit run app.py`
3. 等待应用启动，查看终端输出的URL
4. 应用将在浏览器中运行，用户可以配置参数并执行回测

